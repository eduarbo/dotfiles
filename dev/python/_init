#!/usr/bin/env zsh
source ${0:A:h}/../../deploy
source env.bash

link() {
  mklink config/flake8 $XDG_CONFIG_HOME
}

install() {
  [[ $(_os) == macos ]] && brew install pyenv
}

# update() {}

clean() {
  rm -fv $XDG_CONFIG_HOME/flake8
}

init "$@"
