# -*- mode: snippet -*-
# name: React component
# key: com
# --
import React from 'react';

const ${1:`(let ((base-file-name (f-base buffer-file-name)) (base-directory-name (f-base (file-name-directory buffer-file-name)))) (if (string= base-file-name "index") base-directory-name base-file-name))`} = (props) => (
  $0
);

export default $1;