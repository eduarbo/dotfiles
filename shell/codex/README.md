# Codex CLI

Installs the official `@openai/codex` package through the managed Node toolchain:

```bash
dot shell/codex
```

Authentication remains machine-local. Run `codex login`, complete browser authentication,
and disable credit auto-reload before enabling Codex in Personal Ops. Never copy Codex auth or
session stores from another computer.
